class carts {
    constructor(id, name, imageUri, width, length, distance) {
        this.id = id;
        this.name = name;
        this.imageUri = imageUri;
        this.width = width;
        this.length = length;
        this.distance = distance;
    }
}

export default carts;